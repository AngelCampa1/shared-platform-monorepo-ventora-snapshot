export function Hero() {
  return (
    <section>
      <h1>Close deals faster without busy work</h1>
      <p>Our comprehensive platform helps teams optimize workflows across every deal.</p>
      <code>const value = await getConfig()</code>
    </section>
  );
}
