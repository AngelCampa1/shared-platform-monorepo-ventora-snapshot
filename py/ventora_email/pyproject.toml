[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ventora-email"
version = "0.1.2"
description = "Resend email client with template rendering, unsubscribe tokens, and CAN-SPAM helpers"
license = { text = "UNLICENSED" }
requires-python = ">=3.12"
dependencies = [
  "httpx>=0.27.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0",
  "pytest-asyncio>=0.24.0",
  "pytest-cov>=5.0.0",
  "respx>=0.21.0",
  "ruff>=0.8.0",
  "mypy>=1.13.0",
  "hatchling",
]

[tool.hatch.build.targets.wheel]
packages = ["src/ventora_email"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.coverage.run]
source = ["src/ventora_email"]
omit = ["tests/*"]

[tool.coverage.report]
fail_under = 95
show_missing = true
